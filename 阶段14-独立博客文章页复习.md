# 阶段 14：独立博客文章页复习

## 这一阶段完成了什么

本阶段把首页的三篇博客预览，连接到了三个真正的文章页面：

- `posts/first-webpage.html`
- `posts/why-coding.html`
- `posts/beginner-tips.html`

点击首页的“阅读记录”后，会打开完整文章。文章页底部还有“返回学习记录”链接。

## 重要原理

### 1. 一个 HTML 文件就是一个页面

网站不一定只有一个 `index.html`。每个 HTML 文件都可以是一张独立网页。

### 2. 相对路径

首页在项目根目录，所以链接文章时写：

```html
<a href="posts/first-webpage.html">阅读记录</a>
```

文章页面在 `posts` 文件夹里，要返回上一级目录，使用 `../`：

```html
<a href="../index.html#blog-title">返回学习记录</a>
```

同理，文章页引用根目录的 CSS 和 JavaScript 时，也要使用：

```html
<link rel="stylesheet" href="../style.css?v=14">
<script src="../script.js"></script>
```

### 3. 复用同一套样式

三个文章页都引用同一个 `style.css`，所以它们会保持与首页一致的颜色、字体、深色模式和响应式布局。

## 为什么修改 `script.js`

首页有头像放大功能，文章页没有头像。原来的脚本默认认为每个页面都有头像按钮，因此文章页会出现找不到元素的问题。

现在脚本会先检查元素是否存在，再绑定对应功能。这样同一个脚本就可以安全地复用于首页和文章页。

## 如何复现本阶段

1. 新建 `posts` 文件夹。
2. 在里面创建文章 HTML 文件。
3. 在首页使用 `posts/文件名.html` 链接文章。
4. 在文章页使用 `../index.html` 返回首页。
5. 让文章页引用 `../style.css` 和 `../script.js`。

## 本阶段练习

1. 修改一篇文章的内容，换成你自己的真实经历。
2. 新建一篇文章，例如 `posts/my-guitar.html`。
3. 在首页新增一张文章卡片，并链接到新页面。
4. 在文章页测试深色模式和手机显示效果。

## 发布更新

上传或更新：

- `index.html`
- `style.css`
- `script.js`
- `posts` 文件夹中的三个 HTML 文件
- 本复习文档

## 记住这句话

**相对路径描述的是“从当前文件出发，怎样找到目标文件”。**
