# 阶段 8：学习经历时间线复习

## 这一阶段完成了什么

我们增加了“我的学习足迹”区域，展示三个学习阶段：

1. 开始接触电脑
2. 开始学 HTML
3. 做出个人网站

页面使用了一条竖线和三个圆点，让经历按照顺序排列。

## 重要原理

### 1. 为什么使用有序列表

时间线中的经历有先后顺序，所以使用：

```html
<ol class="timeline">
```

`ol` 表示有序列表，比普通的 `div` 更准确地表达“这些内容有顺序”。

每个阶段使用：

```html
<li class="timeline-item">
```

`li` 表示列表中的一项。

### 2. 用伪元素绘制竖线

```css
.timeline::before {
  position: absolute;
  top: 8px;
  bottom: 8px;
  width: 2px;
}
```

`::before` 是 CSS 伪元素。它不需要额外写一个 HTML 标签，就可以生成装饰性的竖线。

### 3. `position: relative` 和 `absolute`

时间线容器设置：

```css
.timeline {
  position: relative;
}
```

竖线设置：

```css
.timeline::before {
  position: absolute;
}
```

这样竖线就会以 `.timeline` 为参照进行定位，而不是跑到整个页面的其他位置。

### 4. 用圆点表示节点

每个阶段都有一个：

```html
<span class="timeline-marker" aria-hidden="true"></span>
```

CSS 设置宽高、边框和圆角后，就能显示成圆点。`aria-hidden="true"` 表示这个装饰圆点不需要被屏幕阅读器重复读出来。

## 如何复现本阶段

1. 用 `<ol>` 创建有顺序的列表。
2. 每个经历使用一个 `<li>`。
3. 给列表设置 `position: relative`。
4. 用 `::before` 画出竖线。
5. 用额外的 `span` 画出节点。
6. 为移动端检查左侧间距和文字换行。

## 本阶段练习

1. 把三个阶段的描述改成更真实的内容。
2. 再添加一个新的学习阶段。
3. 修改时间线圆点的颜色。
4. 切换深色模式，观察时间线是否仍然清晰。

## 发布更新

本阶段需要上传：

- `index.html`
- `style.css`

不需要修改 `script.js`。

## 记住这句话

**HTML 表达内容的意义，CSS 负责把内容组织成容易阅读的视觉结构。**
