# 阶段 20：自定义 404 页面复习

## 这一阶段完成了什么

本阶段创建了一个自定义的 `404.html` 页面。

当访问者打开不存在的地址时，GitHub Pages 会显示这个页面，而不是使用默认的错误提示。

页面提供了：

- 404 错误提示
- 返回首页按钮
- 查看学习记录链接
- 与网站一致的颜色和排版
- 深色模式
- 手机端适配

## 重要原理

### 1. 什么是 404

HTTP 状态码 `404` 表示服务器找不到访问者请求的页面。

例如访问一个不存在的地址：

```text
https://iqob99.github.io/my-life-website/not-found.html
```

GitHub Pages 会寻找项目根目录中的 `404.html` 并显示它。

### 2. 为什么文件必须叫 `404.html`

这是 GitHub Pages 约定的特殊文件名。只要文件位于网站发布目录的根目录，GitHub Pages 就会自动把它当作错误页面。

### 3. 复用网站样式

```html
<link rel="stylesheet" href="style.css?v=20">
```

`404.html` 和 `index.html` 在同一个目录，所以直接引用 `style.css` 和 `script.js`，不需要使用 `../`。

### 4. 错误页面也应该帮助用户继续浏览

一个好的 404 页面不应该只告诉用户“出错了”，还应该给出下一步选择：

- 返回首页
- 查看博客文章
- 使用导航菜单

## 如何复现本阶段

1. 在项目根目录创建 `404.html`。
2. 写入基本 HTML 结构。
3. 引用现有的 CSS 和 JavaScript。
4. 添加返回首页和主要内容区域的链接。
5. 上传到 GitHub Pages。
6. 手动访问一个不存在的地址进行测试。

## 本阶段练习

1. 把错误提示改成你自己的语气。
2. 给 404 页面增加一张图片或插图。
3. 添加一个返回上一页的按钮。
4. 访问不存在的文章地址，确认 404 页面能够出现。

## 发布更新

上传：

- `404.html`
- `style.css`
- 本复习文档

## 记住这句话

**错误页面也是网站体验的一部分，它应该帮助访问者继续前进。**
