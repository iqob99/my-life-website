# 阶段 9：真实留言表单复习

## 这一阶段完成了什么

我们使用 Formspree 为网站增加了真实留言表单：

- 访问者可以填写名字、邮箱和留言。
- 浏览器会检查必填项目。
- 点击提交后，数据会发送到 Formspree。
- Formspree 会按照账号设置处理和通知留言。

## 重要原理

### 1. 表单结构

```html
<form action="..." method="POST">
  <label for="name">你的名字</label>
  <input id="name" name="name" type="text">
</form>
```

- `form`：表示一个表单。
- `action`：提交数据要发送到哪里。
- `method="POST"`：用 POST 方法发送表单内容。
- `label`：告诉用户输入框是填写什么的。
- `input`：单行输入框。
- `textarea`：多行文字区域。

### 2. `label` 和 `for`

```html
<label for="email">你的邮箱</label>
<input id="email" name="email" type="email">
```

`for="email"` 和 `id="email"` 对应起来。点击文字时，浏览器会自动选中输入框，也方便屏幕阅读器理解表单。

### 3. 浏览器内置校验

```html
<input type="email" required>
```

- `required`：这个输入框不能为空。
- `type="email"`：浏览器会检查内容是否像一个邮箱地址。

这不是完整的安全保护，但可以帮助用户及时发现明显错误。

### 4. Formspree 的作用

静态网站本身不能直接保存留言，因为它没有后端服务器。Formspree 提供了一个接收地址：

```html
<form action="https://formspree.io/f/xqpkjgvj" method="POST">
```

提交后，Formspree 接收内容，并根据你的账号设置转发或保存。

## 如何复现本阶段

1. 在 Formspree 注册账号。
2. 创建一个表单并复制专属地址。
3. 把地址放进 `form` 的 `action`。
4. 为每个输入框设置 `name`。
5. 使用 `required` 和正确的 `type`。
6. 用测试内容提交一次。

## 安全和隐私提醒

- 不要把 Formspree 登录密码写进代码。
- `action` 地址可以出现在公开网页中，这是正常的。
- 不要要求访问者填写身份证号、密码或其他敏感信息。
- 在 Formspree 后台设置垃圾留言过滤和邮件通知。
- 公开网站收到的内容可能包含个人信息，要谨慎保存和处理。

## 发布更新

上传：

- `index.html`
- `style.css`

本阶段不需要修改 `script.js`。

## 记住这句话

**HTML 负责收集输入，Formspree 负责接收静态网站提交的数据。**
