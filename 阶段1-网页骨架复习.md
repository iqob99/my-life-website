# 阶段 1：网页骨架复习

## 这一阶段完成了什么

我们创建了一个最小但完整的个人网站：

- `index.html`：网页的内容和结构。
- `style.css`：网页的颜色、间距、字体和布局。
- 网页可以直接用浏览器打开，不需要安装复杂工具。

## 重要原理

### 1. HTML 是“内容和结构”

HTML 可以理解成网页的骨架。它使用一个个标签告诉浏览器：

- `<h1>` 是最重要的标题。
- `<p>` 是一段文字。
- `<a>` 是链接。
- `<section>` 是一个内容区域。

例如：

```html
<h1>你好，我是你的名字</h1>
```

浏览器读到这行后，就会把文字显示成一级标题。

### 2. CSS 是“外观和布局”

CSS 可以理解成网页的衣服。它负责颜色、字体、大小、间距和位置。

例如：

```css
h1 {
  font-size: 5rem;
}
```

这表示把 `h1` 标题设置得更大。

### 3. HTML 和 CSS 如何连接

`index.html` 的这一行：

```html
<link rel="stylesheet" href="style.css">
```

告诉浏览器：“请把同一个文件夹里的 `style.css` 也加载进来。”

因为两个文件在同一个文件夹，所以只需要写 `style.css`，不需要写复杂路径。

## 如何复现本阶段

1. 新建一个文件夹，例如 `my-life-website`。
2. 在文件夹里新建 `index.html`，复制本项目中的 HTML 内容。
3. 在同一个文件夹里新建 `style.css`，复制本项目中的 CSS 内容。
4. 双击 `index.html`，浏览器就会打开网页。
5. 修改 `h1` 中的文字并保存。
6. 回到浏览器按 `Ctrl + R` 刷新，就能看到修改结果。

## 本阶段练习

请自己修改以下内容，然后刷新浏览器检查结果：

1. 把“你的名字”换成自己的名字。
2. 把介绍文字改成一句真实的自我介绍。
3. 把 `.eyebrow` 的文字换成你喜欢的短句。
4. 把 `#b45309` 换成另一个颜色，例如 `#2563eb`。

## 记住这句话

**HTML 决定“有什么”，CSS 决定“长什么样”。**
