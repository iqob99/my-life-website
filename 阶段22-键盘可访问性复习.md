# 阶段 22：键盘可访问性复习

## 这一阶段完成了什么

本阶段为首页、文章页和 404 页面添加了“跳到主要内容”快捷链接。

使用键盘访问网站时，按下 `Tab` 后会先看到这个链接。按下回车，就可以直接跳过顶部导航，进入页面主要内容。

## 重要原理

### 1. 使用跳转链接

```html
<a class="skip-link" href="#main-content">跳到主要内容</a>
```

链接中的 `#main-content` 表示跳转到页面中 `id="main-content"` 的元素。

### 2. 给主要内容设置唯一 ID

```html
<main id="main-content" class="page">
```

`id` 必须在同一个页面中唯一，这样浏览器才能准确找到目标位置。

### 3. 默认隐藏，获得焦点时显示

```css
.skip-link {
  transform: translateY(-160%);
}

.skip-link:focus {
  transform: translateY(0);
}
```

鼠标浏览页面时，快捷链接不会占据视觉空间；键盘访问时，它会在获得焦点后出现。

## 为什么可访问性重要

有些用户不使用鼠标，而是使用键盘、屏幕阅读器或其他辅助工具浏览网页。

如果顶部导航链接很多，用户每次打开页面都要按很多次 `Tab` 才能进入正文。跳过链接可以减少重复操作。

## 如何复现本阶段

1. 在 `body` 开头添加跳过链接。
2. 在主要内容元素上设置唯一的 `id`。
3. 默认把跳过链接移出可视区域。
4. 使用 `:focus` 在键盘聚焦时显示它。
5. 按 `Tab` 和回车测试跳转是否正确。

## 本阶段练习

1. 打开首页，连续按 `Tab`，观察快捷链接。
2. 按回车，确认焦点跳到主要内容。
3. 给文章页的导航增加其他有意义的无障碍标签。
4. 检查深色模式下跳过链接是否仍然清晰。

## 发布更新

上传：

- `index.html`
- `404.html`
- `posts` 文件夹中的三个 HTML 文件
- `style.css`
- 本复习文档

## 记住这句话

**好的网站不仅要适合鼠标操作，也要适合键盘和辅助工具使用。**
