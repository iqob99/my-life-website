# 阶段 10：表单样式复习

## 这一阶段完成了什么

我们没有改变留言表单的功能，只改善了它的使用体验：

- 姓名和邮箱在电脑上并排显示。
- 手机屏幕上自动变成上下排列。
- 表单拥有独立的卡片背景。
- 标签、输入框和提交按钮之间的间距更清晰。
- 用户输入格式错误时，输入框会显示红色边框。

## 重要原理

### 1. 用容器组织相关字段

姓名和邮箱属于同一行，所以放进：

```html
<div class="form-row">
  ...
</div>
```

每个字段又放进自己的：

```html
<div class="form-field">
  <label>...</label>
  <input>
</div>
```

这样可以分别控制“整行布局”和“单个字段内部间距”。

### 2. 两列布局变成一列

电脑上：

```css
.form-row {
  grid-template-columns: repeat(2, 1fr);
}
```

手机上：

```css
@media (max-width: 640px) {
  .form-row {
    grid-template-columns: 1fr;
  }
}
```

这就是响应式设计：同一份 HTML，根据屏幕宽度使用不同布局。

### 3. `:user-invalid`

```css
input:user-invalid {
  border-color: #dc2626;
}
```

当用户已经操作过输入框，并且内容不符合 `required` 或 `type="email"` 的要求时，浏览器会应用这个样式。

它只是视觉提示，真正的接收和安全检查仍然应该由 Formspree 等服务完成。

### 4. 为什么不使用固定宽度

输入框使用：

```css
width: 100%;
```

它会填满所在容器，而不是写死一个像素宽度。这样在不同屏幕尺寸中更容易适配。

## 如何复现本阶段

1. 把相关表单字段放进共同容器。
2. 用 CSS Grid 创建两列。
3. 在媒体查询中改成一列。
4. 给表单添加背景、边框和圆角。
5. 用 `:user-invalid` 增加错误提示样式。

## 本阶段练习

1. 修改表单卡片的圆角大小。
2. 修改输入框的边框颜色。
3. 缩小浏览器窗口，观察姓名和邮箱是否变成上下排列。
4. 不填写邮箱就提交，观察浏览器提示。
5. 切换深色模式，检查表单是否仍然清晰。

## 发布更新

上传：

- `index.html`
- `style.css`

不需要修改 `script.js`，也不需要重新配置 Formspree。

## 记住这句话

**好的表单不仅能提交，还应该让用户知道该填什么、哪里出错以及如何操作。**
