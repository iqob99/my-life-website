# 阶段 17：回到顶部按钮复习

## 这一阶段完成了什么

本阶段在三篇文章页增加了“回到顶部”按钮。

读者向下滚动超过一定距离后，按钮会出现；点击按钮后，页面会回到顶部。

## 重要原理

### 1. 先隐藏按钮

按钮默认使用透明度和鼠标事件隐藏：

```css
.back-to-top {
  opacity: 0;
  pointer-events: none;
}
```

这样按钮虽然存在于 HTML 中，但不会遮挡页面，也不能被误点击。

### 2. 滚动后显示按钮

JavaScript 根据滚动距离添加 CSS 类：

```javascript
backToTop.classList.toggle("is-visible", window.scrollY > 400);
```

当滚动距离超过 400 像素时，按钮获得 `is-visible` 类，CSS 让它显示出来。

### 3. 回到页面顶部

```javascript
window.scrollTo({
  top: 0,
  behavior: "smooth",
});
```

`top: 0` 表示页面最顶部，`smooth` 表示平滑滚动。

### 4. 尊重用户的动态效果设置

如果用户的系统设置为减少动态效果，JavaScript 会使用普通跳转，而不是平滑动画：

```javascript
window.matchMedia("(prefers-reduced-motion: reduce)").matches
```

这可以让网站对不同用户更加友好。

## 为什么要检查元素是否存在

首页没有回到顶部按钮，但首页和文章页共用 `script.js`。

因此脚本先判断 `.back-to-top` 是否存在，避免首页产生错误。

## 如何复现本阶段

1. 在页面中添加一个按钮。
2. 用 CSS 默认隐藏它。
3. 监听滚动距离。
4. 超过指定距离时显示按钮。
5. 点击按钮后使用 `window.scrollTo()` 回到顶部。
6. 在手机宽度下调整按钮位置。

## 本阶段练习

1. 把按钮文字改成“返回顶部”。
2. 把显示阈值从 400 改成 200。
3. 修改按钮颜色。
4. 测试首页没有按钮时，控制台是否没有错误。

## 发布更新

上传：

- `script.js`
- `style.css`
- `posts` 文件夹中的三个 HTML 文件
- 本复习文档

## 记住这句话

**交互功能通常由 HTML 元素、CSS 状态和 JavaScript 事件共同完成。**
