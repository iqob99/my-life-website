# 阶段 2：个人内容复习

## 这一阶段完成了什么

我们把示例首页改成了 Momk 的个人介绍页：

- 页面标题和描述使用了 Momk。
- 首页主标题改为“正在学习编程的普通人”。
- 增加了“关于我”区域。
- 增加了三个兴趣卡片，用来展示对吉他的热爱。
- 增加了手机屏幕下的适配规则。

## 重要原理

### 1. 用 `id` 创建页面内跳转

按钮写成：

```html
<a href="#about">认识 Momk</a>
```

目标区域写成：

```html
<section id="about">
```

点击按钮时，浏览器会滚动到 `id="about"` 的位置。`#` 表示“在当前页面中寻找这个 id”。

### 2. 用 `class` 复用样式

三个卡片都使用了：

```html
<article class="interest-card">
```

CSS 只需要写一次 `.interest-card`，三个卡片就会拥有相同的外观。`class` 适合给多个元素套用同一套样式。

### 3. `grid` 负责排列卡片

```css
.interest-list {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
}
```

- `display: grid`：开启网格布局。
- `repeat(3, 1fr)`：分成 3 列，每列平均分配空间。
- `gap: 16px`：卡片之间留出 16 像素的距离。

### 4. 媒体查询适配手机

```css
@media (max-width: 640px) {
  .interest-list {
    grid-template-columns: 1fr;
  }
}
```

当屏幕宽度小于等于 640 像素时，三列会变成一列。这样手机上不会因为卡片太窄而难以阅读。

## 如何复现本阶段

1. 打开 `index.html`。
2. 在 `<main>` 中准备主标题、介绍、关于我和兴趣区域。
3. 给需要跳转的区域设置唯一的 `id`。
4. 给重复结构使用相同的 `class`。
5. 用 CSS Grid 排列卡片。
6. 用浏览器窗口缩窄，检查手机布局是否变成一列。

## 本阶段练习

1. 修改“关于我”中的文字，让它更像真实的你。
2. 为三个兴趣卡片分别写一句不同的描述。
3. 把按钮文字改成你喜欢的表达。
4. 拖动浏览器窗口变窄，观察卡片布局变化。

## 记住这句话

**`id` 用来定位一个地方，`class` 用来给一组元素复用样式。**
