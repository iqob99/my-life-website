# 阶段 12：页面访问动画复习

## 这一阶段完成了什么

我们给网站增加了温和的页面进入动画：

- 顶部导航会淡入并轻微上移。
- 主内容会稍后淡入，形成自然的先后顺序。
- 动画只使用 CSS，不需要额外的 JavaScript。
- 用户系统设置“减少动态效果”时，动画会自动关闭。

## 重要原理

### 1. `@keyframes` 定义动画过程

```css
@keyframes page-enter {
  from {
    opacity: 0;
    transform: translateY(16px);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

- `from`：动画开始时的状态。
- `to`：动画结束时的状态。
- `opacity`：透明度，`0` 是完全透明，`1` 是完全不透明。
- `translateY`：沿垂直方向移动。

### 2. 使用 `animation`

```css
.page {
  animation: page-enter 700ms 120ms ease both;
}
```

这几个值依次表示：

1. 动画名称：`page-enter`
2. 动画时长：`700ms`
3. 延迟时间：`120ms`
4. 速度曲线：`ease`
5. 填充方式：`both`

延迟让导航先出现，主内容后出现。

### 3. 为什么不能滥用动画

动画应该帮助用户理解页面层次，而不是让内容难以阅读。我们只使用一次短暂的进入动画，没有让文字无限闪烁或持续移动。

### 4. `prefers-reduced-motion`

```css
@media (prefers-reduced-motion: reduce) {
  .site-header,
  .page {
    animation: none;
  }
}
```

部分用户会在系统中开启“减少动态效果”，可能是因为容易晕动、注意力敏感或无障碍需求。网页应该尊重这个设置。

## 如何复现本阶段

1. 用 `@keyframes` 定义开始和结束状态。
2. 给需要动画的元素添加 `animation`。
3. 用延迟制造自然的出现顺序。
4. 用 `prefers-reduced-motion` 关闭动画。
5. 刷新页面，观察动画是否只播放一次。

## 本阶段练习

1. 把动画时长改成 `1000ms`，观察变化。
2. 把主内容延迟改成 `300ms`。
3. 修改 `translateY(16px)` 的数值。
4. 在浏览器开发者工具或系统设置中模拟减少动态效果。

## 发布更新

上传：

- `index.html`
- `style.css`

`index.html` 中的样式链接使用了版本号 `style.css?v=12`，这样浏览器可以识别到新版本，避免继续使用旧缓存。

本阶段不需要修改 `script.js`。

## 记住这句话

**好的动画是内容的辅助，而不是内容的主角。**
