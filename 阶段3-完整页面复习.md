# 阶段 3：完整页面复习

## 这一阶段完成了什么

我们把首页补充成了更完整的个人网站：

- 顶部增加了 Logo 和导航。
- 导航可以跳转到“关于我”“兴趣”“联系”。
- 增加了感谢访问的联系区域。
- 增加了页脚和版权文字。
- 保留了手机屏幕适配。
- 整体使用温暖、安静的视觉风格。

## 重要原理

### 1. 页面通常有三个主要区域

```html
<header>顶部区域</header>
<main>主要内容</main>
<footer>底部信息</footer>
```

- `header`：通常放 Logo、导航和页面顶部内容。
- `main`：放当前页面最重要的内容。
- `footer`：放版权、补充链接等信息。

这样写不仅更容易阅读，也能帮助搜索引擎和辅助工具理解页面结构。

### 2. 导航就是多个页面内链接

```html
<a href="#interests">兴趣</a>
```

它会寻找页面中 `id="interests"` 的元素。导航不一定要跳到新网页，也可以跳到当前页面的不同区域。

### 3. `:hover` 和 `:focus-visible`

```css
nav a:hover,
nav a:focus-visible {
  color: #b45309;
}
```

- `:hover`：鼠标移动到链接上时改变样式。
- `:focus-visible`：键盘操作选中链接时改变样式。

同时写这两个状态，可以让鼠标用户和键盘用户都知道当前选中了什么。

### 4. `max-width` 让内容更容易阅读

页面内容没有无限拉宽，而是使用：

```css
width: min(680px, calc(100% - 40px));
```

意思是：内容最多 680 像素，但在小屏幕上两侧仍保留 20 像素空间。文字行太长会难以阅读，所以限制宽度是一种重要的排版方法。

## 如何复现本阶段

1. 在 `body` 顶部加入 `header` 和导航。
2. 为每个目标区域设置唯一的 `id`。
3. 在主要内容后加入 `footer`。
4. 用 `display: flex` 排列 Logo 和导航。
5. 用 `:hover` 和 `:focus-visible` 增加交互反馈。
6. 缩小浏览器窗口，检查导航和卡片是否仍然清晰。

## 本阶段练习

1. 把导航中的“联系”改成你喜欢的文字。
2. 给 Logo 的 `Momk.` 换一个你喜欢的写法。
3. 修改 `.contact` 的背景色，观察整体气质变化。
4. 用键盘按 `Tab`，观察导航链接是否能被看到。

## 记住这句话

**完整页面不仅要有内容，还要有清晰的结构、导航和反馈。**
