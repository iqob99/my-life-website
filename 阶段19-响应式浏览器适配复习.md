# 阶段 19：响应式浏览器适配复习

## 这一阶段解决了什么问题

之前在较窄的浏览器窗口中，顶部的 Logo、导航和深色模式按钮可能互相挤压。

本阶段重新整理了响应式布局，让首页和文章页都能适应：

- 电脑浏览器
- 平板浏览器
- 手机浏览器
- 很窄的浏览器窗口

## 重要原理

### 1. 使用媒体查询

```css
@media (max-width: 640px) {
  /* 手机和平板较窄窗口的样式 */
}
```

当浏览器宽度小于 640px 时，CSS 会应用这一组规则。

### 2. 手机端重新排列顶部区域

手机端使用 Grid：

```css
.site-header {
  display: grid;
  grid-template-columns: 1fr auto;
}
```

这样 Logo 和主题按钮在第一行，导航自动放到下一行，不会互相挤压。

### 3. 允许导航换行

```css
nav {
  flex-wrap: wrap;
}
```

如果浏览器太窄，导航链接可以换到下一行，而不是超出屏幕。

### 4. 防止横向滚动

```css
body {
  overflow-x: hidden;
}
```

同时，文章导航还使用：

```css
overflow-wrap: anywhere;
```

这样较长的文章标题也可以在窄屏中换行。

### 5. 使用多个断点

本项目现在有两个窄屏断点：

- `max-width: 640px`：调整主要手机布局。
- `max-width: 420px`：进一步缩小间距、标题和表单内边距。

不同屏幕尺寸不一定需要完全相同的样式，断点可以帮助我们逐步调整。

## 如何测试响应式页面

1. 打开浏览器开发者工具。
2. 点击“切换设备工具栏”。
3. 分别测试手机、平板和桌面尺寸。
4. 检查是否出现横向滚动条。
5. 检查标题、导航和按钮是否被截断。
6. 检查表单输入框是否完整显示。

## 本阶段练习

1. 把 `640px` 改成 `700px`，观察布局变化。
2. 把手机端导航改成居中排列。
3. 尝试为超小屏幕设置更小的按钮间距。
4. 在浏览器中测试 320px、390px、768px 和 1024px 宽度。

## 发布更新

上传：

- `index.html`
- `style.css`
- `posts` 文件夹中的三个 HTML 文件
- 本复习文档

## 记住这句话

**响应式设计不是让所有屏幕看起来一样，而是让每种屏幕都容易使用。**
