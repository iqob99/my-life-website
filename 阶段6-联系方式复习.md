# 阶段 6：添加联系方式复习

## 这一阶段完成了什么

我们在“联系”区域加入了一个可点击的邮箱链接：

```html
<a href="mailto:123456789@qq.com">
  发送邮件给我：123456789@qq.com
</a>
```

现在访问者点击链接后，设备会尝试打开默认邮件应用，并自动填写收件人。

## 重要原理

### 1. `mailto` 链接

普通网页链接使用：

```html
<a href="https://example.com">打开网站</a>
```

邮箱链接使用：

```html
<a href="mailto:name@example.com">发送邮件</a>
```

`mailto:` 会告诉操作系统：这是一个邮箱地址，而不是普通网页地址。

### 2. 为什么邮箱要写两次

在下面的代码里：

```html
<a href="mailto:name@example.com">name@example.com</a>
```

- `href` 中的邮箱决定点击后发送给谁。
- 标签中显示的邮箱让访问者知道地址是什么。

这两处必须保持一致，否则用户看到的地址和实际收件地址可能不同。

### 3. 链接的交互状态

我们给邮箱链接增加了：

```css
.email-link:hover,
.email-link:focus-visible {
  text-decoration: underline;
}
```

鼠标移动到链接上，或者用键盘选中它时，会显示下划线，让用户知道它可以点击。

## 如何复现本阶段

1. 在联系区域添加一个 `<a>` 标签。
2. 把 `href` 写成 `mailto:你的邮箱`。
3. 在标签文字中显示邮箱。
4. 用 CSS 设置颜色和鼠标悬停效果。
5. 点击本地网页中的链接，检查系统是否打开邮件应用。

## 发布更新

本地检查无误后，重新上传：

- `index.html`
- `style.css`

本阶段没有新增图片文件。

## 隐私提醒

公开邮箱可能收到垃圾邮件。如果以后收到较多垃圾邮件，可以：

- 为网站单独注册一个公开邮箱。
- 使用联系表单服务。
- 暂时隐藏邮箱，只保留社交平台链接。

不要在公开网站上放密码、验证码、身份证号码或家庭住址。

## 记住这句话

**`mailto:` 让网页链接连接到邮件应用，而不是另一个网页。**
