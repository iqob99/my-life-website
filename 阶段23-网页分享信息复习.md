# 阶段 23：网页分享信息复习

## 这一阶段完成了什么

本阶段为首页、三篇博客文章和 404 页面补充了网页分享信息。

现在搜索引擎和社交平台更容易理解每个页面的：

- 标题
- 摘要
- 页面类型
- 页面地址
- 网站名称
- 文章发布日期

## 重要原理

### 1. `title` 和 `description`

```html
<title>Momk | 个人网站</title>
<meta name="description" content="Momk 的个人网站，记录编程学习和对吉他的热爱。">
```

`title` 通常显示在浏览器标签页，`description` 可以作为搜索结果摘要。

### 2. Open Graph 信息

```html
<meta property="og:title" content="Momk | 个人网站">
<meta property="og:description" content="Momk 的个人网站，记录编程学习和对吉他的热爱。">
<meta property="og:type" content="website">
```

`og:` 元信息常被社交平台用于生成分享卡片。

文章页面使用：

```html
<meta property="og:type" content="article">
```

首页使用：

```html
<meta property="og:type" content="website">
```

### 3. canonical 页面地址

```html
<link rel="canonical" href="https://iqob99.github.io/my-life-website/">
```

`canonical` 告诉搜索引擎哪个是页面的主要正式地址，减少带有查询参数的地址造成的重复判断。

### 4. 文章发布日期

文章页面增加了：

```html
<meta property="article:published_time" content="2026-09-01">
```

这样分享工具或其他程序可以知道文章发布时间。

## 如何复现本阶段

1. 为页面写清楚的 `title`。
2. 为页面写准确的 `description`。
3. 添加 `og:title`、`og:description` 和 `og:type`。
4. 添加页面的正式 URL。
5. 文章页面添加发布日期。
6. 用查看源代码的方式检查元信息。

## 本阶段练习

1. 修改首页的分享摘要。
2. 给文章补充 `og:image` 分享图片。
3. 比较首页和文章页的 `og:type`。
4. 使用浏览器查看页面源代码，寻找所有 `<meta>` 标签。

## 发布更新

上传：

- `index.html`
- `404.html`
- `posts` 文件夹中的三个 HTML 文件
- 本复习文档

## 记住这句话

**网页不仅要让浏览器看懂，也要让搜索引擎和分享平台看懂。**
