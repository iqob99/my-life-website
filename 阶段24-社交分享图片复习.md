# 阶段 24：社交分享图片复习

## 这一阶段完成了什么

本阶段为首页、三篇文章和 404 页面添加了社交分享图片。

当页面分享到支持 Open Graph 或 Twitter Card 的平台时，平台可以使用个人照片作为分享缩略图。

## 重要原理

### 1. Open Graph 图片

```html
<meta
  property="og:image"
  content="https://iqob99.github.io/my-life-website/pic1.jpg"
>
```

图片地址使用完整的 HTTPS 地址，因为社交平台需要从互联网上读取图片。

### 2. Twitter Card

```html
<meta name="twitter:card" content="summary_large_image">
<meta
  name="twitter:image"
  content="https://iqob99.github.io/my-life-website/pic1.jpg"
>
```

这组信息可以帮助支持 Twitter Card 格式的平台生成大图分享卡片。

### 3. 为什么所有页面使用同一张图片

目前网站只有一张个人照片，所以首页、文章页和 404 页面统一使用 `pic1.jpg`。

以后如果为每篇文章制作专门的封面图，可以把每个页面的 `og:image` 改成不同地址。

## 如何复现本阶段

1. 确认图片已经上传到网站。
2. 使用 HTTPS 绝对地址引用图片。
3. 添加 `og:image`。
4. 添加 `twitter:card` 和 `twitter:image`。
5. 上传页面并等待 GitHub Pages 更新。
6. 查看页面源代码，确认元信息存在。

## 本阶段练习

1. 为某篇文章制作一张专属封面图。
2. 把该文章的 `og:image` 改成新的图片地址。
3. 检查图片地址能否直接在浏览器打开。
4. 比较首页和文章页分享信息的不同。

## 发布更新

上传：

- `index.html`
- `404.html`
- `posts` 文件夹中的三个 HTML 文件
- `pic1.jpg`
- 本复习文档

## 记住这句话

**分享信息不仅决定文字，也决定别人看到的第一张图片。**
