# 阶段 5：添加图片复习

## 这一阶段完成了什么

我们把 `pic1.jpg` 添加到了首页介绍区域，并做成了圆形头像效果。

## 重要原理

### 1. 图片文件和网页文件的关系

HTML 中写：

```html
<img src="pic1.jpg" alt="Momk 的个人照片">
```

- `src` 是图片地址。
- `pic1.jpg` 表示图片和 `index.html` 在同一个文件夹。
- `alt` 是图片无法显示时的替代文字，也方便使用屏幕阅读器的人理解图片内容。

如果图片放在 `images` 文件夹里，路径就要写成：

```html
<img src="images/pic1.jpg" alt="Momk 的个人照片">
```

### 2. `object-fit: cover`

```css
object-fit: cover;
```

它会让图片填满设定的宽高，同时尽量保持图片比例。多出来的部分会被裁掉，因此适合做头像或卡片封面。

### 3. 圆形图片

```css
border-radius: 50%;
```

当图片的宽高相同并设置为 `50%` 时，就会显示成圆形。

## 如何复现本阶段

1. 把图片复制到和 `index.html` 相同的文件夹。
2. 确认图片文件名和 `src` 中完全一致，包括扩展名大小写。
3. 给图片添加准确的 `alt` 描述。
4. 设置图片宽高和 `object-fit`。
5. 重新打开网页，确认图片能显示。

## 常见问题

### 图片显示不出来

检查以下内容：

- 文件是否真的在项目文件夹中。
- 文件名是否拼写正确。
- `.jpg`、`.jpeg`、`.png` 是否写对。
- 图片是否被命名成了 `pic1.jpg.jpg`。

## 发布更新

本地图片能显示后，还要把 `pic1.jpg` 一起上传到 GitHub 仓库，否则线上网站找不到图片。

上传时要同时提交：

- 修改后的 `index.html`
- 修改后的 `style.css`
- 新增的 `pic1.jpg`

## 记住这句话

**网页不仅要让看得见的人看懂，也要让无法看到图片的人知道图片表达了什么。**
